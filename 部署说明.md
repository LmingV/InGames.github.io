# InGames Wiki 部署说明

这是一套已经配置好的 Docusaurus 项目，推送到 GitHub 后会**自动构建并发布**到你的自定义域名 `https://InGames.wiki`。

## 一次性设置（只做一次）

### 1. 清理沙盒残留的 .git 文件夹

我在准备环境时留下了一个损坏的 `.git` 文件夹（沙盒权限限制无法删除）。在你自己的电脑上，进入项目目录先删掉它：

- Windows PowerShell：`Remove-Item -Recurse -Force .git`
- Mac/Linux：`rm -rf .git`

### 2. 本地安装依赖并预览（可选，但建议）

```bash
npm install
npm start
```

浏览器打开 `http://localhost:3000` 就能看到站点。确认没问题后按 `Ctrl+C` 停止。

> `npm install` 会生成 `package-lock.json`，建议一起提交，这样线上构建更快更稳定。

### 3. 初始化 Git 并推送到 GitHub

```bash
git init
git branch -M main
git add .
git commit -m "初始化 Docusaurus 站点"
git remote add origin https://github.com/LmingV/InGames.github.io.git
git push -u origin main
```

### 4. 在 GitHub 仓库开启 Pages（关键）

1. 打开仓库页面 → **Settings**（设置）
2. 左侧菜单选 **Pages**
3. 在 **Build and deployment** → **Source** 处，选择 **GitHub Actions**（不是 "Deploy from a branch"）

设置好后，第 3 步的推送会触发 Actions 自动构建。在仓库的 **Actions** 标签页可以看到构建进度。

### 5. 绑定自定义域名 InGames.wiki

项目里已经放好了 `static/CNAME` 文件（内容是 `InGames.wiki`），构建时会自动带上。你还需要在**域名服务商**处配置 DNS：

- 若用**子域名**（如 `www.InGames.wiki`）：添加一条 `CNAME` 记录，指向 `LmingV.github.io`
- 若用**根域名**（`InGames.wiki`）：添加 4 条 `A` 记录，分别指向：
  - `185.199.108.153`
  - `185.199.109.153`
  - `185.199.110.153`
  - `185.199.111.153`

DNS 生效后，在仓库 **Settings → Pages → Custom domain** 处填入 `InGames.wiki`，并勾选 **Enforce HTTPS**。

## 日常使用：更新内容后自动同步

以后每次改内容，只需三条命令：

```bash
git add .
git commit -m "更新了某某内容"
git push
```

推送到 `main` 分支后，GitHub Actions 会自动构建并发布，一两分钟后 `https://InGames.wiki` 就更新了。**无需任何手动部署操作。**

## 添加新文档

在 `docs/` 目录下新建 `.md` 文件即可，侧边栏会自动生成。文件开头可以用 `sidebar_position` 控制排序：

```markdown
---
sidebar_position: 2
---

# 页面标题

正文……
```

## 项目结构说明

| 路径 | 作用 |
|------|------|
| `docs/` | 文档内容（Markdown） |
| `blog/` | 博客文章 |
| `src/pages/` | 自定义页面（如首页） |
| `src/css/custom.css` | 全局样式 |
| `static/` | 静态资源，含 `CNAME` 和图片 |
| `docusaurus.config.js` | 站点主配置 |
| `sidebars.js` | 侧边栏配置 |
| `.github/workflows/deploy.yml` | 自动部署工作流 |

## 常见问题

**Q：推送后 Actions 报错怎么办？**
到仓库 Actions 标签页点开失败的任务看日志。最常见是第 4 步没把 Source 设为 "GitHub Actions"。

**Q：域名一直不生效？**
DNS 记录可能需要几分钟到几小时传播。可以用 `nslookup InGames.wiki` 检查解析是否指向 GitHub。

**Q：想换个主题色？**
改 `src/css/custom.css` 里的 `--ifm-color-primary` 系列变量即可。
